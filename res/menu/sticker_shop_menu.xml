<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" >

    <item
        android:id="@+id/shop_search"
        app:showAsAction="always"
        android:title="@string/shop_search_hint"
        android:visible="false"
        app:actionLayout="@layout/shop_search_layout"/>

    <item
        android:id="@+id/shop_settings"
        android:icon="@drawable/ic_sticker_settings"
        app:showAsAction="always"
        android:title="@string/my_stickers"/>

</menu>