<?xml version="1.0" encoding="UTF-8"?>
<resources>
	<declare-styleable name="Kpt_Atx_Keyboard_Key">
		<attr name="kpt_keyLabel2" format="string" />
	</declare-styleable>
	
	<declare-styleable name="Kpt_Atx_KeyboardView">
		<attr name="kpt_keyTextColor2" format="color" />
		<attr name="kpt_keyTextSize2" format="dimension" />
		<attr name="kpt_keyTextSize4" format="dimension" />
		<attr name="kpt_keyTextSize3" format="dimension" />
		 <!-- Color(Secondary text) to use for the label in a key in dark theme -->
		<attr name="kpt_keyTextColor3" format="color" />
	</declare-styleable>
	
	<declare-styleable name="Kpt_KeyboardViewPreviewState">
        <!-- State for {@link android.inputmethodservice.KeyboardView KeyboardView}
                key preview background. -->
    </declare-styleable>
    
    <declare-styleable name="Kpt_Theme">
    	<attr name="kpt_backgroundDimAmount" format="float" />
    </declare-styleable>
	
	<declare-styleable name="Kpt_KeyboardView">
        <!-- Default KeyboardView style. -->
        <attr name="kpt_keyboardViewStyle" format="reference" />

        <!-- Image for the key. This image needs to be a StateListDrawable, with the following
             possible states: normal, pressed, checkable, checkable+pressed, checkable+checked,
             checkable+checked+pressed. -->
        <attr name="kpt_keyBackground" format="reference" />

        <!-- Size of the text for character keys. -->
        <attr name="kpt_keyTextSize" format="dimension" />

        <!-- Size of the text for custom keys with some text and no icon. -->
        <attr name="kpt_labelTextSize" format="dimension" />

        <!-- Color to use for the label in a key. -->
        <attr name="kpt_keyTextColor" format="color" />
        
         <!-- Color(Primary text) to use for the label in a key in dark theme -->
        <attr name="kpt_keyTextColor1" format="color" />

        <!-- Layout resource for key press feedback.-->
        <attr name="kpt_keyPreviewLayout" format="reference" />

        <!-- Vertical offset of the key press feedback from the key. -->
        <attr name="kpt_keyPreviewOffset" format="dimension" />

        <!-- Height of the key press feedback popup. -->
        <attr name="kpt_keyPreviewHeight" format="dimension" />

        <!-- Amount to offset the touch Y coordinate by, for bias correction. -->
        <attr name="kpt_verticalCorrection" format="dimension" />

        <!-- Layout resource for popup keyboards. -->
        <attr name="kpt_popupLayout" format="reference" />

        <attr name="kpt_shadowColor" format="color" />
    	<attr name="kpt_shadowRadius" format="float"/>
    </declare-styleable>
	
	
	 <declare-styleable name="Kpt_Keyboard">
        <!-- Default width of a key, in pixels or percentage of display width. -->
        <attr name="kpt_keyWidth" format="dimension|fraction" />
        <!-- Default height of a key, in pixels or percentage of display width. -->
        <attr name="kpt_keyHeight" format="dimension|fraction" />
        <!-- Default horizontal gap between keys. -->
        <attr name="kpt_horizontalGap" format="dimension|fraction" />
        <!-- Default vertical gap between rows of keys. -->
        <attr name="kpt_verticalGap" format="dimension|fraction" />
        
        <attr name="kpt_keyLabelXPos" format="float" />
        <attr name="kpt_keyLabelYPos" format="float" />
		<attr name="kpt_keyLabel2XPos" format="float" />
		<attr name="kpt_keyLabel2YPos" format="float" />
		
		<attr name="kpt_keyLabelTextSize" format="dimension" />
		<attr name="kpt_keyLabel2TextSize" format="dimension" />
        
		<attr name="kpt_languageType" format="integer" />
		<attr name="kpt_isRTL" format="boolean" />
		<attr name="kpt_fontStyleApplicable" format="boolean" />
		<attr name="kpt_fontSizeApplicable" format="boolean" />
		    
		<attr name="kpt_keyType" format="integer"  />
		<attr name="kpt_isGlideSupported" format="boolean"  />
		
    </declare-styleable>

    <declare-styleable name="Kpt_Keyboard_Row">
        <!-- Row edge flags. -->
        <attr name="kpt_rowEdgeFlags">
            <!-- Row is anchored to the top of the keyboard. -->
            <flag name="top" value="4" />
            <!-- Row is anchored to the bottom of the keyboard. -->
            <flag name="bottom" value="8" />
        </attr>
        <!-- Mode of the keyboard. If the mode doesn't match the
             requested keyboard mode, the row will be skipped. -->
        <attr name="kpt_keyboardMode" format="reference" />
        <attr name="kpt_isNumberRow" format="boolean" />
    </declare-styleable>
	
	 <declare-styleable name="Kpt_Keyboard_Key">
        <!-- The unicode value or comma-separated values that this key outputs. -->
        <attr name="kpt_codes" format="integer|string" />
        <!-- The XML keyboard layout of any popup keyboard. -->
        <attr name="kpt_popupKeyboard" format="reference" />
        <!-- The characters to display in the popup keyboard. -->
        <attr name="kpt_popupCharacters" format="string" />
        <!-- Key edge flags. -->
        <attr name="kpt_keyEdgeFlags">
            <!-- Key is anchored to the left of the keyboard. -->
            <flag name="left" value="1" />
            <!-- Key is anchored to the right of the keyboard. -->
            <flag name="right" value="2" />
        </attr>
        <!-- Whether this is a modifier key such as Alt or Shift. -->
        <attr name="kpt_isModifier" format="boolean" />
        <!-- Whether this is a toggle key. -->
        <attr name="kpt_isSticky" format="boolean" />
        <!-- Whether long-pressing on this key will make it repeat. -->
        <attr name="kpt_isRepeatable" format="boolean" />
        <!-- The icon to show in the popup preview. -->
        <attr name="kpt_iconPreview" format="reference" />
        <!-- The string of characters to output when this key is pressed. -->
        <attr name="kpt_keyOutputText" format="string" />
        <!-- The label to display on the key. -->
        <attr name="kpt_keyLabel" format="string" />
        <!-- The icon to display on the key instead of the label. -->
        <attr name="kpt_keyIcon" format="reference" />
        
        <attr name="kpt_useExternalFont" format="boolean" />
        <attr name="kpt_iconText" format="string" />
        
        <!-- Mode of the keyboard. If the mode doesn't match the
             requested keyboard mode, the key will be skipped. -->
        <attr name="kpt_keyboardMode" />
	
        <!-- the special operation of the key -->
        <attr name="kpt_keySpecialOper" format="integer" />
        
        <!-- index of the charactar in the hindhi aplhabet. -->
        <attr name="kpt_syllable_index" format="integer" />
        
        <!-- unicode of the vowel in the conjuction charactar in the hindhi aplhabet. -->
        <attr name="kpt_vowel_unicode" format="integer" />
        
        <!-- all the conjuction charactars that are formed after that particular vowel added to all the charactars in the hindhi aplhabet. -->
        <attr name="kpt_vowel_base" format="string" />
        
    </declare-styleable>
		
	
	
	<declare-styleable name="Kpt_ImageListPreference">
		<attr name="kpt_entryImages" format="reference" />
	</declare-styleable>
	
	  <declare-styleable name="Kpt_IconPreferenceScreen">
        <attr name="kpt_kptIcon" format="reference" />
    </declare-styleable>
	
	
</resources>